你是符合公司开发规范的资深工程师，现在根据以下文档内容生成可直接运行的Web项目代码：

---

## *一.产品需求*

#### 1.产品核心需求:
- 

#### 2.视觉与设计规范:
- 

#### 3.核心功能列表:
- {总结核心需求}
- 
#### 4.功能页面清单
- 需要哪些页面

#### 5.接口服务清单
- 需要哪些接口
---
## *二.技术栈选型*
| 技术类别 | 选型方案 | 版本要求 | 使用说明 |
|----------|----------|----------|----------|
| 框架 | Next.js | 最新版本（App Router） | 统一使用App Router路由模式，禁止混合Pages Router，前后端一体 |
| 样式方案 | Tailwind CSS + CSS Modules | Tailwind ≥3.x | 全局样式写在globals.css，组件样式独立文件（xxx.module.css） |
| 数据库 | Supabase | 最新稳定版 | 数据存储、身份认证（Auth）、实时订阅功能使用 |
| 部署平台 | Vercel | 无（按平台最新支持） | main分支 |
| 认证工具 | Google OAuth + Supabase Auth | 无 | 统一使用第三方登录，禁止自建账号体系（除非项目特殊要求） |
| 接口请求 | Supabase JS Client + SWR | Supabase JS ≥2.x | 服务端请求用Supabase Server Client，客户端用SWR缓存 |
| 语言 | TypeScript |  | 使用TypeScript完成页面开发与自定义接口服务 |
| UI技术库 | Tailwind CSS、Shadcn UI、Lucide、React (图标库)样式、Animation（动效） |  | 使用TypeScript完成页面开发与自定义接口服务 |
| 支付 | whop api、vercel |  | 使用whop完成的支付功能 |

---

## *三.UI 样式具体使用*
根据我给的 {地址} UI设计规范文档进行生成符合的产品

---

## *四.配置信息清单*
- supabase:
- google auth:
- vercel:

---

## *五.开发规范与注意事项*
1. 代码严格遵循ESLint标准通用规范，ESLint+Prettier 自动格式化,无语法错误；
2. 注释规范：组件 / 工具函数需写 JSDoc 注释（参数、返回值、用途），复杂逻辑加行内注释；
3. 版本控制规范：main
4. 命名规范：文件 / 组件命名采用 PascalCase（如 CustomerCard.tsx），函数 / 变量采用 camelCase（如 getCustomerList），常量采用 UPPER_SNAKE_CASE（如 MAX_PAGE_SIZE）；
---